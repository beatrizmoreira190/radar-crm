'use client';
import { useEffect, useMemo, useState } from 'react';
import { Activity, Search, X } from 'lucide-react';
import Avatar from '@/components/Avatar';
import { useCrm } from '@/components/CrmProvider';
import { timeAgo } from '@/lib/constants';

export default function ActivityPage(){const {supabase,membership,teamMap,activityVersion,isManager,user}=useCrm();const org=membership?.organization_id;const [rows,setRows]=useState([]);const [q,setQ]=useState('');const [loading,setLoading]=useState(true);const [notice,setNotice]=useState('');async function load(){if(!org)return;setLoading(true);let query=supabase.from('audit_events').select('id,actor_user_id,entity_type,entity_id,action,label,before_data,after_data,created_at').eq('organization_id',org);if(!isManager)query=query.eq('actor_user_id',user?.id);const {data,error}=await query.order('created_at',{ascending:false}).limit(250);if(error)setNotice(error.message);setRows(data||[]);setLoading(false)}useEffect(()=>{load()},[org,user?.id,isManager,activityVersion]);const filtered=useMemo(()=>{const n=q.trim().toLowerCase();return n?rows.filter(r=>`${r.label} ${r.action} ${r.entity_type} ${r.after_data?.name||''} ${r.after_data?.title||''}`.toLowerCase().includes(n)):rows},[rows,q]);function actor(id){return teamMap[id]||{full_name:'Sistema'}}function context(a){return a.after_data?.name||a.after_data?.title||a.after_data?.summary||a.before_data?.name||a.before_data?.title||''}return <div className="page-wrap"><div className="page-head"><div><div className="eyebrow">Histórico</div><h1>Atividade</h1><p>{isManager?'Acompanhe as principais alterações registradas pela equipe no CRM.':'Revise as alterações e ações que você registrou no CRM.'}</p></div></div>{notice&&<div className="notice-bar"><span>{notice}</span><button onClick={()=>setNotice('')}><X size={15}/></button></div>}<div className="toolbar"><div className="search-box"><Search size={17}/><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar no histórico"/></div></div><section className="card panel">{loading?<div className="table-empty">Carregando atividade…</div>:filtered.length?<div className="activity-list">{filtered.map(a=><div className="activity-row" key={a.id}><Avatar member={actor(a.actor_user_id)} size={36}/><div><strong>{actor(a.actor_user_id).full_name||actor(a.actor_user_id).email||'Sistema'}</strong><span>{a.label||a.action}{context(a)?` · ${String(context(a)).slice(0,90)}`:''}</span><small>{timeAgo(a.created_at)} · {a.entity_type||'registro'}</small></div></div>)}</div>:<div className="empty-state"><Activity/><strong>Nenhuma atividade encontrada.</strong><p>{isManager?'As movimentações da equipe aparecerão aqui.':'Suas movimentações aparecerão aqui.'}</p></div>}</section></div>}
