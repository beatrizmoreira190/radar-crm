'use client';
import { useEffect, useState } from 'react';
import { Check, Copy, MessageSquareText, Plus, X } from 'lucide-react';
import { useCrm } from '@/components/CrmProvider';
import { CHANNEL_LABELS } from '@/lib/constants';

export default function Templates(){const {supabase,membership,user,isManager}=useCrm();const org=membership?.organization_id;const [rows,setRows]=useState([]);const [copied,setCopied]=useState('');const [show,setShow]=useState(false);const [notice,setNotice]=useState('');async function load(){if(!org)return;const {data}=await supabase.from('outreach_templates').select('*').eq('organization_id',org).eq('active',true).order('name');setRows(data||[])}useEffect(()=>{load()},[org]);async function copy(t){await navigator.clipboard.writeText(t.body);setCopied(t.id);setTimeout(()=>setCopied(''),1400)}return <div className="page-wrap"><div className="page-head"><div><div className="eyebrow">Biblioteca comercial</div><h1>Modelos de abordagem</h1><p>Roteiros e mensagens para manter consistência sem robotizar a conversa.</p></div>{isManager&&<button className="btn" onClick={()=>setShow(true)}><Plus size={16}/> Novo modelo</button>}</div>{notice&&<div className="notice-bar"><span>{notice}</span><button onClick={()=>setNotice('')}><X size={15}/></button></div>}<div className="template-grid">{rows.map(t=><article className="card template-card" key={t.id}><div className="template-top"><div className="template-icon"><MessageSquareText/></div><div><strong>{t.name}</strong><span>{CHANNEL_LABELS[t.channel]||t.channel}{t.purpose?` · ${t.purpose}`:''}</span></div></div><div className="template-body">{t.body}</div><button className="btn secondary full" onClick={()=>copy(t)}>{copied===t.id?<><Check size={16}/> Copiado</>:<><Copy size={16}/> Copiar texto</>}</button></article>)}</div><div className="card panel manual-tip"><h3>Como usar os modelos</h3><p>Substitua campos como <code>[NOME]</code> e <code>[SEU NOME]</code>, adapte o contexto e registre no CRM o que realmente aconteceu. Modelo é ponto de partida — não substitui escuta comercial.</p></div>{show&&<TemplateModal org={org} user={user} supabase={supabase} onClose={()=>setShow(false)} onSaved={()=>{setShow(false);load();setNotice('Modelo criado.')}}/>}</div>}
function TemplateModal({org,user,supabase,onClose,onSaved}){const [f,setF]=useState({name:'',channel:'email',purpose:'',body:''});const [err,setErr]=useState('');function s(k,v){setF(x=>({...x,[k]:v}))}async function save(e){e.preventDefault();const {error}=await supabase.from('outreach_templates').insert({organization_id:org,...f,created_by:user.id,updated_by:user.id});if(error)setErr(error.message);else onSaved()}return <div className="modal-backdrop"><form className="modal" onSubmit={save}><div className="modal-head"><div><h3>Novo modelo</h3><p>Disponível para toda a equipe Radar.</p></div><button type="button" onClick={onClose}><X/></button></div>{err&&<div className="notice error">{err}</div>}<div className="form-grid"><label className="span-2">Nome<input className="input" required value={f.name} onChange={e=>s('name',e.target.value)}/></label><label>Canal<select value={f.channel} onChange={e=>s('channel',e.target.value)}><option value="phone">Ligação</option><option value="email">E-mail</option><option value="whatsapp">WhatsApp</option><option value="linkedin">LinkedIn</option><option value="meeting">Reunião</option><option value="other">Outro</option></select></label><label>Finalidade<input className="input" value={f.purpose} onChange={e=>s('purpose',e.target.value)} placeholder="Primeiro contato, follow-up…"/></label><label className="span-2">Texto<textarea rows={8} required value={f.body} onChange={e=>s('body',e.target.value)}/></label></div><div className="modal-actions"><button type="button" className="btn secondary" onClick={onClose}>Cancelar</button><button className="btn">Salvar modelo</button></div></form></div>}
