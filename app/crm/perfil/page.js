'use client';
import { useEffect, useState } from 'react';
import { Camera, Save } from 'lucide-react';
import { useCrm } from '@/components/CrmProvider';
import Avatar from '@/components/Avatar';
import { ROLE_LABELS } from '@/lib/constants';

export default function ProfilePage(){const {supabase,user,membership,refreshTeam,refresh}=useCrm();const [name,setName]=useState(membership?.full_name||'');const [job,setJob]=useState(membership?.job_title||'');const [avatar,setAvatar]=useState(membership?.avatar_url||'');const [busy,setBusy]=useState(false);const [notice,setNotice]=useState('');useEffect(()=>{setName(membership?.full_name||'');setJob(membership?.job_title||'');setAvatar(membership?.avatar_url||'')},[membership]);async function upload(e){const file=e.target.files?.[0];if(!file)return;setBusy(true);setNotice('');const ext=(file.name.split('.').pop()||'jpg').toLowerCase();const path=`${user.id}/${Date.now()}.${ext}`;const {error}=await supabase.storage.from('crm-avatars').upload(path,file,{upsert:true});if(error){setNotice(error.message);setBusy(false);return;}const {data}=supabase.storage.from('crm-avatars').getPublicUrl(path);setAvatar(data.publicUrl);setBusy(false)}async function save(){setBusy(true);setNotice('');const {error}=await supabase.from('org_members').update({full_name:name,job_title:job||null,avatar_url:avatar||null,updated_at:new Date().toISOString()}).eq('organization_id',membership.organization_id).eq('user_id',user.id);if(error)setNotice(error.message);else{setNotice('Perfil atualizado.');await refreshTeam();await refresh();}setBusy(false)}return <div className="page-wrap narrow"><div className="page-head"><div><div className="eyebrow">Conta</div><h1>Meu perfil</h1><p>Atualize seu nome, função e foto de perfil.</p></div></div><section className="card profile-card"><div className="profile-avatar-area"><Avatar member={{...membership,full_name:name,avatar_url:avatar}} size={104}/><label className="avatar-upload"><Camera size={16}/> Alterar foto<input type="file" accept="image/png,image/jpeg,image/webp" onChange={upload}/></label></div><div className="profile-fields"><label>Nome completo<input className="input" value={name} onChange={e=>setName(e.target.value)}/></label><label>Cargo / função<input className="input" value={job} onChange={e=>setJob(e.target.value)} placeholder="Ex.: Prospecção comercial"/></label><label>E-mail<input className="input" disabled value={user?.email||membership?.email||''}/></label><label>Perfil de acesso<input className="input" disabled value={ROLE_LABELS[membership?.role]||membership?.role}/></label>{notice&&<div className="notice success">{notice}</div>}<button className="btn" disabled={busy} onClick={save}><Save size={16}/>{busy?'Salvando…':'Salvar perfil'}</button></div></section></div>}
